# Connectly API - Activity 6: CRUD Operations

This branch contains the initial setup for the Connectly API, focusing on the core **CRUD (Create, Read, Update, Delete)** functionalities.

## 🛠️ Core Models
* **User**: Basic user profile containing username and email fields.
* **Post**: Content storage linked to a user.

## 🚀 Features
* **CBVs (Class-Based Views)**: Implemented `APIView` for handling GET and POST requests.
* **Serialization**: Basic conversion of model instances to JSON data.
* **Endpoint Routing**: Established the initial URL structure for the `posts` app.

## 🧪 Initial Testing
Basic testing for creating users and posts via Postman to ensure database connectivity and successful response handling.
